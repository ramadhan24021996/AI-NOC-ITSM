# MCP (Model Context Protocol)

**Status:** Unknown / Not explicitly identified in the core architecture.

The system relies on direct API calls to LLM providers (Gemini, Groq) via standard REST/SDK interfaces and uses NATS for internal microservice communication rather than MCP.
